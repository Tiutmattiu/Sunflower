import assert from 'node:assert/strict';import {advanceHarbourWindow,createHarbourWorld} from '../src/harbourSpine.js';import {performPlayerAction as act,visitLocation as go} from '../src/playerGame.js';import {diagnosePlayer,MARKET_SCARS} from '../src/playerDiagnosis.js';const day=w=>advanceHarbourWindow(w),to=(w,n)=>{while(w.day<n)w=day(w);return w};
let gap=createHarbourWorld();gap=go(gap,'joels_bar');gap=act(gap,'trade_bridge');gap=day(gap);gap=act(gap,'intermediate_lead');gap=act(gap,'trade_bridge');
let run=createHarbourWorld();run=go(run,'joels_bar');run=act(run,'trade_bridge');for(let i=0;i<3;i++){run=day(run);run=go(run,'public_clearing');run=act(run,'misstate_public_listing')}run=act(run,'fire_sale');
let rival=createHarbourWorld();rival=to(rival,2);rival=go(rival,'viewing_room');rival=act(rival,'auction_preview');rival=to(rival,4);rival=act(rival,'auction_provenance');rival=act(rival,'share_provenance');rival=to(rival,6);rival=act(rival,'auction_bid',{amount:1});rival=day(rival);
const runs={gap,run,rival},required=['FIRST THROUGH THE GAP','FIRE SALE','SOLD THE MAP','TAUGHT YOUR RIVAL','RUN ON YOU'],proof={};for(const name of required){for(const [runName,w] of Object.entries(runs)){const scar=diagnosePlayer(w).scars.find(s=>s.name===name);if(scar)proof[name]={classification:'A',run:runName,evidenceIds:scar.evidenceIds,sequence:scar.types}}assert(proof[name],`${name} not causally reproduced`)}const classification=Object.fromEntries(MARKET_SCARS.map(s=>[s.name,{classification:s.coverage==='A'?'A':'B',reproduced:proof[s.name]||null,causalTypes:s.types}]));assert(!Object.values(classification).some(x=>['C','D'].includes(x.classification)));console.log(JSON.stringify({proof,classification},null,2));
